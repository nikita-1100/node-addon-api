{
  "name": "emit-event-from-cpp-example",
  "version": "0.0.0",
  "description": "Node.js Addons - emit event from C++ to JS",
  "main": "index.js",
  "private": true,
  "gypfile": true,
  "scripts": {
    "start": "npm i && node index.js && start http://localhost:3000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-addon-api": "*",
    "bindings": "*"
  }
}
